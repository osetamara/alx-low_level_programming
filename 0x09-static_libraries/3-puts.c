#include "main.h"
void _puts(char *str)
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	puts("\"programming is like buliding a multilingual puzzle");
	return (0);
}
