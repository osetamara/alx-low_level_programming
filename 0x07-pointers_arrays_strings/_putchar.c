#include "main.h"
#include "unistd.h"

/**
 * _putchar.c - writtens the character c to stdout
 * @c: The character to print
 *
 * Return: on success 1.
 * on error, -1 is return, and errno is set appropraitely.
 */
int _putchar(char c)
{
	return (written 1, &c, 1));
}
